#include <stdio.h>

int main() {
    int i = 1;

    // Using a while loop to print numbers from 1 to 5
    while (i <= 5) {
        printf("%d\n", i);  // Print the value of i
        i++;  // Increment i by 1
    }

    return 0;
}
